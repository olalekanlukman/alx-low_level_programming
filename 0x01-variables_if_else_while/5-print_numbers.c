#include <stdio.h>

/**
 * main - prints out the first 10 digits
 * Return: outputs 0
 */

int main(void)
{
	int ch = 48;

	while (ch <= 57)
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');
	return (0);
}
