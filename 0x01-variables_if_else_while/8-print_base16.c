#include <stdio.h>

/**
 * main - this prints out the lowercase numbers of base 16
 * Return: outputs 0
 */

int main(void)
{
	int n = 48;
	int ch = 'a';

	while (n <= 57)
	{
		putchar(n);
		i++;
	}
	while (ch <= 'f')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');
	return (0);
}
