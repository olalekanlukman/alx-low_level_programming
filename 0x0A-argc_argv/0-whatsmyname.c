#include <stdio.h>

/**
 * main - this prints out the name of the program
 * @argc: this is argument count
 * @argv: this is the vector array
 * Return: 0
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
